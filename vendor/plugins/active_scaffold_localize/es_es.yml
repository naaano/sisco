"es_es":
  locale_name: "Spanish"
  active_scaffold:
    add:                  "Agregar"
    add_existing:         "Agregar Existente"
    are_you_sure:         "Esta seguro/a?"
    cancel:               "Cancelar"
    click_to_edit:        "Click para editar"
    close:                "Cerrar"
    create:               "Crear"
    create_model:         "Crear {{model}}"
    create_another:       "Crear Otro"
    created_model:        "{{model}} creado"
    create_new:           "Crear Nuevo"
    customize:            "Customizar"
    delete:               "Eliminar"
    deleted_model:        "{{model}} eliminado"
    delimiter:            "Delimitador"
    download:             "Descargar"
    edit:                 "Editar"
    export:               "Exportar"
    filtered:             "(Filtrado)"
    found:                "Encontrado(s)"
    hide:                 "Esconder"
    live_search:          "Busqueda en vivo"
    loading…:             "Cargando…"
    nested_for_model:     "{{nested_model}} de {{parent_model}}"
    next:                 "Siguiente"
    no_entries:           "Sin entradas"
    omit_header:          "Omitir Encabezado"
    options:              "Opciones"
    pdf:                  "PDF"
    previous:             "Previo"
    print:                "Imprimir"
    refresh:              "Refrescar"
    remove:               "Remover"
    remove_file:          "Remover o Reemplazar archivo"
    replace_with_new:     "Reemplazar Con Otro Nuevo"
    revisions_for_model:  "Revisiones para {{model}}"
    reset:                "Restablecer"
    saving…:              "Guardando…"
    search:               "Buscar"
    search_terms:         "Términos de Búsqueda"
    _select_:             "- seleccione -"
    show:                 "Ver"
    show_model:           "Ver {{model}}"
    _to_ :                " a "
    update:               "Actualizar"
    update_model:         "Actualizar {{model}}"
    udated_model:         "{{model}} actualizado"

    percentage_example:   "Ex. 10%"
    usa_phone_example:    "Ex. 111-333-4444"
    usa_money_example:    "Ex. 1,333"
    usa_zip_example:      "Ex. 88888-3333"
    ssn_example:          "Ex. 555-22-3333"
    
    # error_messages
    internal_error: "Oops! aqui hay algo que no me cuadra bien (codigo 500, Error interno)"
    version_inconsistency: "Inconsistencia de Versión - este registro ha sido modificado desde que usted empezó a editarlo."
  date:
    formats:
      # Use the strftime parameters for formats.
      # When no format has been given, it uses default.
      # You can provide other formats here if you like!
      default: "%d-%m-%Y"
      short: "%b %d"
      long: "%B %d, %Y"
      
    day_names: [Domingo, Lunes, Martes, Miércoles, Jueves, Viernes, Sábado]
    abbr_day_names: [Dom, Lun, Mar, Mie, Jue, Vie, Sab]
      
    # Don't forget the nil at the beginning; there's no such thing as a 0th month
    month_names: [~, Enero, Febrero, Marzo, Abril, Mayo, Junio, Julio, Agosto, Septiembre, Octubre, Noviembre, Diciembre]
    abbr_month_names: [~, Ene, Feb, Mar, Abr, May, Jun, Jul, Ago, Sep, Oct, Nov, Dic]
    # Used in date_select and datime_select.
    order: [ :day, :month, :year ]

  time:
    formats:
      default: "%a, %d %b %Y %H:%M:%S %z"
      short: "%d %b %H:%M"
      long: "%B %d, %Y %H:%M"
    am: "am"
    pm: "pm"
      
  # Used in array.to_sentence.
  support:
    array:
      sentence_connector: "y"
      skip_last_comma: false
  number:
    # Used in number_with_delimiter()
    # These are also the defaults for 'currency', 'percentage', 'precision', and 'human'
    format:
      # Sets the separator between the units, for more precision (e.g. 1.0 / 2.0 == 0.5)
      separator: ","
      # Delimets thousands (e.g. 1,000,000 is a million) (always in groups of three)
      delimiter: "."
      # Number of decimals, behind the separator (the number 1 with a precision of 2 gives: 1.00)
      precision: 2
      
    # Used in number_to_currency()
    currency:
      format:
        # Where is the currency sign? %u is the currency unit, %n the number (default: $5.00)
        format: "%u%n"
        unit: "$"
        # These three are to override number.format and are optional
        separator: ","
        delimiter: "."
        precision: 2
        
    # Used in number_to_percentage()
    percentage:
      format:
        # These three are to override number.format and are optional
        # separator: 
        delimiter: ""
        # precision: 
        
    # Used in number_to_precision()
    precision:
      format:
        # These three are to override number.format and are optional
        # separator:
        delimiter: ""
        # precision:
        
    # Used in number_to_human_size()
    human:
      format:
        # These three are to override number.format and are optional
        # separator: 
        delimiter: ""
        precision: 1

  # Used in distance_of_time_in_words(), distance_of_time_in_words_to_now(), time_ago_in_words()
  datetime:
    distance_in_words:
      half_a_minute: "medio minuto"
      less_than_x_seconds:
        one:   "menos de 1 segundo"
        other: "menos de {{count}} segundos"
      x_seconds:
        one:   "1 segundo"
        other: "{{count}} segundos"
      less_than_x_minutes:
        one:   "menos de 1 minuto"
        other: "menos de {{count}} minutos"
      x_minutes:
        one:   "1 minuto"
        other: "{{count}} minutos"
      about_x_hours:
        one:   "como 1 hora"
        other: "como {{count}} horas"
      x_days:
        one:   "1 dia"
        other: "{{count}} dias"
      about_x_months:
        one:   "como 1 mes"
        other: "como {{count}} meses"
      x_months:
        one:   "1 mes"
        other: "{{count}} meses"
      about_x_years:
        one:   "como 1 año"
        other: "como {{count}} años"
      over_x_years:
        one:   "mas de 1 año"
        other: "mas de {{count}} años"

  activerecord:
    errors:
      template:
        header:
          one:    "1 error prohibió este/a {{model}} de ser guardado"
          other:  "{{count}} errores prohibieron este/a {{model}} de ser guardado"
        # The variable :count is also available
        body: "Hubieron problemas con los siguientes campos:"
      # The values :model, :attribute and :value are always available for interpolation
      # The value :count is available when applicable. Can be used for pluralization.
      messages:
        inclusion: "is not included in the list"
        exclusion: "is reserved"
        invalid: "is invalid"
        confirmation: "doesn't match confirmation"
        accepted: "must be accepted"
        empty: "can't be empty"
        blank: "can't be blank"
        too_long: "is too long (maximum is {{count}} characters)"
        too_short: "is too short (minimum is {{count}} characters)"
        wrong_length: "is the wrong length (should be {{count}} characters)"
        taken: "has already been taken"
        not_a_number: "is not a number"
        greater_than: "must be greater than {{count}}"
        greater_than_or_equal_to: "must be greater than or equal to {{count}}"
        equal_to: "must be equal to {{count}}"
        less_than: "must be less than {{count}}"
        less_than_or_equal_to: "must be less than or equal to {{count}}"
        odd: "must be odd"
        even: "must be even"
        # Append your own errors here or at the model/attributes scope.

      # You can define own errors for models or model attributes.
      # The values :model, :attribute and :value are always available for interpolation.
      #
      # For example,
      #   models:
      #     user:
      #       blank: "This is a custom blank message for {{model}}: {{attribute}}"
      #       attributes:
      #         login:
      #           blank: "This is a custom blank message for User login"
      # Will define custom blank validation message for User model and 
      # custom blank validation message for login attribute of User model.
      models:

    # Translate model names. Used in Model.human_name().
    #models:
      # For example,
      #   user: "Dude"
      # will translate User model name to "Dude"

    # Translate model attribute names. Used in Model.human_attribute_name(attribute).
    #attributes:
      # For example,
      #   user:
      #     login: "Handle"
      # will translate User attribute "login" as "Handle"
